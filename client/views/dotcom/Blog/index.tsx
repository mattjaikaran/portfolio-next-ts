import BlogCard from '@/components/blog/BlogCard';
import styles from './Blog.module.scss';

interface BlogAuthorProps {
  date: Date;
  name: string;
}

const Blog = (props: BlogAuthorProps) => {
  return <div>Blog</div>;
};

export default Blog;
